<!DOCTYPE html>
<html>
<head>

  <meta charset="utf-8">
  <meta http-equiv="X-UA-Compatible" content="IE=edge">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <meta name="description" content="<%= meta_description %>">
  <meta name="author" content="<%= meta_author %>">
  <meta name="Keywords" content="<%= meta_keywords %>">

  <title><%= full_title(yield(:title)) %></title>

  <%= stylesheet_link_tag    "application", media: "all", "data-turbolinks-track" => true %>
  <%= javascript_include_tag "application", "data-turbolinks-track" => true %>
  <%= csrf_meta_tags %>

  <!-- HTML5 shim and Respond.js IE8 support of HTML5 elements and media queries -->
  <!--[if lt IE 9]>
    <script src="https://oss.maxcdn.com/libs/html5shiv/3.7.0/html5shiv.js"></script>
    <script src="https://oss.maxcdn.com/libs/respond.js/1.3.0/respond.min.js"></script>
  <![endif]-->
  <a href="#Top"></a>
</head>
<body>
  <div class="navbar navbar-static-top" role="navigation">
    <div class="container">
      <div class="navbar-header">
        <%= image_tag('RLC_LOGO.png', class: 'pull-left', style: "height: 80px") %>
        <%= link_to site_name, site_url, :class => "navbar-brand", style: 'font-size: 25px; padding-top: 30px; margin-left: 0;' %>
      </div>
      <div class="collapse navbar-collapse" style='padding-top: 21px; font-size: 18px;'>
        <ul class="nav nav-pills pull-right">
          <li class="<%= 'active' if current_page?(calendar_path) %>"><%= link_to "Calendar", calendar_path %></li>
          <li class="<%= 'active' if current_page?(events_path) %>"><%= link_to "Event", events_path %></li>
          <li class="<%= 'active' if current_page?(categories_path) %>"><%= link_to "Category", categories_path %></li>
          <% if user_signed_in? %>
            <li class="<%= 'active' if current_page?(user_path(current_user)) %>"> <%= link_to image_tag(current_user.avatar.url(:square), class: 'img-circle', style:'height: 70px; vertical-align:middle;'), user_path(current_user), style: 'margin-top: -30px;' %></li>
            <li ><%= link_to "Log out", destroy_user_session_path, method: 'delete' %></li>
          <% else %>
            <li> <%= link_to "Log in", new_user_session_path %> </li>
            <li> <%= link_to "Sign up", new_user_registration_path %> </li>
          <% end %>

        </ul>
      </div><!-- /.nav-collapse -->
      <hr/>
    </div><!-- /.container -->

  </div><!-- /.navbar -->

  <div id="warpper">
    <div class="container">
      <% if notice.present? %>
        <p class="alert  alert-success" role="alert"><%= notice %></p>
      <% end %>
      <% if alert.present? %>
        <p class="alert alert-danger" role="alert"><%= alert %></p>
      <% end %>
      <%= yield %>
    </div>
  </div>

  <footer>
     <div class="container">
      <hr>
    </div>
  </footer>
 

</body>
</html>
